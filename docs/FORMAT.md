# MS3V format notes

The `.SMP` workflow here is experimental and was validated against a physical MS3V player.

The converter produces a 22,050 Hz, stereo, 64 kbps MP3 and then applies the experimental byte transformation used by the working converter.

This is **not an official MS3V specification**.

